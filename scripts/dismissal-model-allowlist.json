{
	"src/lib/frontend/actions/use-dismiss.svelte.ts :: click": "Sanctioned non-Bits dismissal implementation; see this file's header comment. Dismisses outside clicks while preserving interactions that began inside and respecting ignored nodes.",
	"src/lib/frontend/actions/use-dismiss.svelte.ts :: keydown": "Sanctioned non-Bits dismissal implementation; see this file's header comment. Handles Escape for the topmost enabled dismisser, with opt-out and IME guards.",
	"src/lib/frontend/actions/use-dismiss.svelte.ts :: pointerdown": "Sanctioned non-Bits dismissal implementation; see this file's header comment. Records where an interaction began so its subsequent click does not dismiss a surface it started inside.",
	"src/lib/frontend/components/file/FileViewer.svelte :: keydown": "Escape-key handler registered only while the file viewer is visible; prevents default and clears the viewer through handleClose. No global outside-click or focus model, though this is independent Escape dismissal retained as existing behavior.",
	"src/lib/frontend/components/input/InputArea.svelte :: keydown": "Escape keyboard shortcut navigates back to the parent session only with empty input and neither command nor file menu visible. It performs session navigation, not overlay dismissal or focus management.",
	"src/lib/frontend/components/layout/ChatLayout.svelte :: keydown": "Window keyboard shortcut Ctrl/Cmd+Shift+D toggles the debug feature. It does not implement outside-click dismissal, Escape handling or focus management.",
	"src/lib/frontend/components/model/AgentSelector.svelte :: click": "Hand-rolled outside-click model that duplicates use-dismiss, checking trigger and portal containment before closing. Pinned only so the ratchet can be turned on today; migration is tracked as conduit-test-79hj.",
	"src/lib/frontend/components/model/AgentSelector.svelte :: keydown": "Escape closing plus ArrowUp/ArrowDown navigation and Enter selection belong to the hand-rolled outside-click model that duplicates use-dismiss. Pinned only so the ratchet can be turned on today; migration is tracked as conduit-test-79hj.",
	"src/lib/frontend/components/model/InstanceModelPicker.svelte :: keydown": "Escape-key handler closes the open picker. Outside-click dismissal uses the sanctioned use-dismiss action with escape: false, so this is the picker-specific Escape branch rather than another outside-click or focus model; it remains independent of the shared Escape stack.",
	"src/lib/frontend/components/model/ModelVariant.svelte :: keydown": "Ctrl+T keyboard shortcut cycles model variants when variants exist. It changes the selected variant and implements no dismissal or focus model.",
	"src/lib/frontend/components/project/ProjectSwitcher.svelte :: keydown": "Escape-key handler cancels the add-project form first, otherwise closes the switcher. Outside clicks use the sanctioned use-dismiss action; this is feature-specific Escape state handling rather than another outside-click or focus model, with overlap in Escape ownership retained as existing behavior."
}
