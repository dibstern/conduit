# Role template: global-contract.

[[steps]]
id = "{{logical_id}}"
title = "{{title}}"
type = "decision"
description = "{{description}}"
needs = {{needs_array}}

[steps.metadata.planToBeads]
contractVersion = "plan-to-beads.v2"
templateVersion = "{{template_version}}"
role = "global-contract"
logicalId = "{{logical_id}}"
planId = "{{plan_id}}"
sourcePlan = "{{source_plan}}"
contextRefs = {{context_refs_array}}
inherits = {{inherits_array}}
typedContractRefs = {{typed_contract_refs_array}}
provides = {{provides_array}}

[steps.metadata.globalContract]
scope = {{scope_array}}
nonGoals = {{non_goals_array}}
repoBoundary = {{repo_boundary_array}}
packageBoundary = {{package_boundary_array}}
successCriteria = {{success_criteria_array}}
globalConstraints = {{global_constraints_array}}
globalForbiddenWork = {{global_forbidden_work_array}}
requiredSkills = {{required_skills_array}}
crossPlanRelationships = {{cross_plan_relationships_array_of_tables}}
executorPolicyRefs = {{executor_policy_refs_array}}
