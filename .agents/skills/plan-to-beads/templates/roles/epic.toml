# Role template: epic / molecule root.
# Hydrate into a root molecule or an epic issue when creating issues manually.

[[steps]]
id = "{{logical_id}}"
title = "{{title}}"
type = "epic"
description = "{{description}}"
needs = {{needs_array}}

[steps.metadata.planToBeads]
contractVersion = "plan-to-beads.v1"
templateVersion = "{{template_version}}"
role = "epic"
logicalId = "{{logical_id}}"
planId = "{{plan_id}}"
sourcePlan = "{{source_plan}}"
contextRefs = {{context_refs_array}}
inherits = {{inherits_array}}
provides = {{provides_array}}

[steps.metadata.epicContract]
summary = "{{summary}}"
successCriteria = {{success_criteria_array}}
nonGoals = {{non_goals_array}}
children = {{children_array}}
checkpoints = {{checkpoints_array}}
