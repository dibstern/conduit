# Role template: policy.

[[steps]]
id = "{{logical_id}}"
title = "{{title}}"
type = "decision"
description = "{{description}}"
needs = {{needs_array}}

[steps.metadata.planToBeads]
contractVersion = "plan-to-beads.v1"
templateVersion = "{{template_version}}"
role = "policy"
logicalId = "{{logical_id}}"
planId = "{{plan_id}}"
sourcePlan = "{{source_plan}}"
contextRefs = {{context_refs_array}}
inherits = {{inherits_array}}
provides = {{provides_array}}

[steps.metadata.policyContract]
policyKind = "{{policy_kind}}"
rules = {{rules_array}}
defaults = {{defaults_table}}
validationCommands = {{validation_commands_array}}
failureConditions = {{failure_conditions_array}}
appliesToRoles = {{applies_to_roles_array}}
