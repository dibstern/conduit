# Role template: pilot / evidence checkpoint.

[[steps]]
id = "{{logical_id}}"
title = "{{title}}"
type = "task"
description = "{{description}}"
needs = {{needs_array}}

[steps.metadata.planToBeads]
contractVersion = "plan-to-beads.v3"
templateVersion = "{{template_version}}"
role = "pilot"
logicalId = "{{logical_id}}"
planId = "{{plan_id}}"
sourcePlan = "{{source_plan}}"
contextRefs = {{context_refs_array}}
inherits = {{inherits_array}}
typedContractRefs = {{typed_contract_refs_array}}
provides = {{provides_array}}

[steps.metadata.pilotContract]
purpose = "{{purpose}}"
commands = {{commands_array}}
metrics = {{metrics_array}}
evidenceOutputs = {{evidence_outputs_array}}
evidenceRuns = {{evidence_runs_array_of_tables}}
operatorSmoke = {{operator_smoke_array_of_tables}}
runbooks = {{runbooks_array_of_tables}}
decisionRules = {{decision_rules_array}}
forbiddenWork = {{forbidden_work_array}}
followupTemplateRefs = {{followup_template_refs_array}}
