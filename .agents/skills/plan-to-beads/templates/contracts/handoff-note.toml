# Contract template: durable Beads handoff note.
# Store this shape in bd notes/metadata, not only in per-worktree artifacts.

beadId = "{{bead_id}}"
logicalId = "{{logical_id}}"
status = "{{status}}"
commitSha = "{{commit_sha}}"
summary = "{{summary}}"
redCommandObserved = "{{red_command_observed}}"
verificationCommand = "{{verification_command}}"
verificationResult = "{{verification_result}}"
verificationResultRefs = {{verification_result_refs_array}}
evidenceRunRefs = {{evidence_run_refs_array}}
evidenceRefs = {{evidence_refs_array}}
filesChanged = {{files_changed_array}}
allowedFilesUsed = {{allowed_files_used_array}}
forbiddenFilesTouched = {{forbidden_files_touched_array}}
artifacts = {{artifacts_array}}
risks = {{risks_array}}
followups = {{followups_array}}
blockedOn = {{blocked_on_array}}
checkpointRefs = {{checkpoint_refs_array}}
