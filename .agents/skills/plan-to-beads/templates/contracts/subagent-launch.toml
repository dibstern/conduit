# Contract template: subagent launch packet.
# Usually embedded in parent or checkpoint metadata.

launchWhen = "{{launch_when}}"
wave = "{{parallel_wave}}"
maxSubagents = {{max_subagents_int}}
claimCommand = "{{claim_command}}"
readyCommand = "{{ready_command}}"
dependencyFreshnessCommand = "{{dependency_freshness_command}}"
worktreePolicy = "{{worktree_policy}}"
branchNamePattern = "{{branch_name_pattern}}"
promptTemplate = "{{prompt_template}}"
contextRefs = {{context_refs_array}}
typedContractRefs = {{typed_contract_refs_array}}
guardrailRefs = {{guardrail_refs_array}}
executorProfileRef = "{{executor_profile_ref}}"
allowedFiles = {{allowed_files_array}}
forbiddenFiles = {{forbidden_files_array}}
handoffRequired = true
handoffLocation = "{{handoff_location}}"
closePolicy = "{{close_policy}}"
mergeCheckpointRef = "{{merge_checkpoint_ref}}"
evidenceRequired = {{evidence_required_array}}
failureConditions = {{failure_conditions_array}}
